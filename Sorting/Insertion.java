package Sorting;

public class Insertion {
    public static void insertion(int[] arr, int n) {
        n = arr.length;
        for (int i = 1; i < n; i++) {
            int key = arr[i];
            int j = i - 1;
        }
        while (j < 0 && arr[j] > key) {

        }
    }
}
